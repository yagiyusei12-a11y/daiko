import { Card } from "../ui";

export default function DailyReportsMenuPage(): JSX.Element {
  return (
    <Card title="日報">
      <p style={{ margin: 0, fontSize: "0.95rem", color: "var(--color-muted)" }}>日報機能はこれから実装します。</p>
    </Card>
  );
}
