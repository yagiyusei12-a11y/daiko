import { Card } from "../ui";

export default function AttendanceMenuPage(): JSX.Element {
  return (
    <Card title="勤怠">
      <p style={{ margin: 0, fontSize: "0.95rem", color: "var(--color-muted)" }}>勤怠機能はこれから実装します。</p>
    </Card>
  );
}
